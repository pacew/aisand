-*- mode:visual-line -*-

I'm pretty new to claude.  I dabbled with it a couple of months ago.  Now that I've come back to it, I'm being asked about an "Auth conflict" and given 2 choices (token or API key).  How do I choose?


I've been using Gemini's chat interface to do some coding projects, with a lot of copy and pasting.  I want to try claude code, but I want to build a simple docker sandbox that I can understand and that will give me some confidence that the model won't be able to do damage to the rest of my laptop.  I want the model to have unlimited access to the external network and to read and write and execute local files in a designated subtree of my host file system.  I'll assume that the model will be subject to malicious (even targeted) prompt injection since it will be reading from external sources, so I'll assume anything it writes to local files might be tainted.  I'll mitigate that risk by making sure never to store an authentication credentials in the subtree (except thoese necessary to make claude code work), and by essentially code reviewing anything it has written before I run it outside of the container.

First, let's refine this strategy.  You can tell me about related work that may already be available and ask me questions to clarify what I want and expose hidden implications.  If it seems like a good idea to implement this, we'll next write a README.md file to record the plan, and then create the docker configuration and launch files.

===

I want Option A, run claude code inside the container.

For threat model, this is a little fanciful but let's try to make it safe against this attack. An attacker stalks me and figures out what kind of project I'm working on.  They figure out a web site where my claude code will likely search for resources for my project.  Maybe it's some kind of online community.  They make a post that effectively turns into an injected prompt for my claude code.  I don't care if this causes my claude code to dump everything it has access to in the subtree or run arbitrary programs inside the container.  I can accept even a fork bomb that crashes the laptop.  But I don't want to allow permanent damage to the laptop outside the container, and I don't want a program running in the container to exfiltrate files from outside.

My work flow will be for the subtree to be a git repository that claude code will treat as strictly local.  I'll run meld outside the container to code review changes, and I'll run git push myself from outside the container.

To clarify network access: yes to full outbound; no to inbound; no to local services (localhost:3000)

I think the only credential needed is the anthropic API key.

I haven't used docker a lot, and I don't know how to choose between persistent volume and ephemeral sandbox.

Thank you for the comments about containing damage - I'm aligned with you on that.

===

I want to use the claude CLI (I use emacs and will never learn vs code).

I do want an interactive shell in the container.  I imagine claude and I will collaborate there for some parts of the projects, and then I'll do the careful review when I need to run something that needs more access to external resources.

persistent volume + ephemeral container sounds good

Using an environment variable for the api key sounds good.  You are currently running in a pretty default configuration on my laptop, so the launch script should pick up the key from the default place.

===

Here's more info on what I want to do: Mostly Python and embedded C projects, plus a little php.  My main laptop runs Pop!_OS 22.04 LTS.  I also use computers with various recent ubuntu's.

So let's use debian as the base image.  I agree with npm install

You mention the idea of having the launch script notice a .git directory and setting up for a particular project - that sounds good to me.  For now, I think the idea of separate claude threads for separate projects sounds fine.  I'm ok with the launch script building the Dockerfile dynamically after it knows the directory, if that's necessary.

I'm going to name this infrastructure project "aisand".  That's the name of the directory you're currently running in, and I'll push it to a public github repository with that name when it's done.  Let's call the launch script "aisand" and the usage will be to run it with the current directory being the root of a repository.

===

Looks real good.  Let's refine README.md first and let the other files dangle until it is settled.

In "What we protect against", you say "Arbitrary command execution inside the container" - but I think we've covered what we need to allow arbitrary commands (for example pytests).

Let's duplicate the directory structure inside the container.  If the project name is in "/home/pace/apply" on the host, give it the same name in the container, instead of /work (and /home/pace will be otherwise empty in the container)

We can make the launch script aisand have the x bit set in the repository, so the README doesn't need to mention chmod

A common usage pattern might be to develop a pytest.  In one window I'll run aisand and claude, and in another, just aisand.  Then either claude or I can run the pytest in the restricted environment.

Let's package those docker commands with "aisand rebuild" and "aisand prune"

Remove the php mentions ... I won't actually run php in the container for that project ... I'll just be generating php to run on the host, so there's no extra infrastructure needed.

My github account is github.com/pacew

I just remembered about MEMORY.md and other claude state storage.  Should we make a standardized place in the project repository for these files?

===

It seems like mounting the host ~/.claude opens an attack vector.  My intuition is to add a .claude-memory to each project's repository.  Recognizing that many of my projects will get pushed to my public github, what kinds of sensitive data might wind up there?

===

Now that I think about it, I don't want to commit .claude-memory to the project repositories.  My main work pattern is use a single personal laptop for personal projects and a single work laptop for work projects.  For now, if I want to work on a project on a different host, I can manually copy .claude-memory to it (and if that becomes common, make a script).

Instead of leaving half thought out ideas about version controlled memory in the readme, just take that stuff out for now.

The aisand project itself doesn't need special memory handling.  I'll never run it in a container, and I'll manually review any commands claude wants to run in it (mainly just file editing).  It can use the default ~/.claude memory.

===

I have a feeling that the automatic claude memory management is probably already good and going to get better, so I want to support it smoothly, even if it means the launch command needs to set up a volume for it.

===

I'm having problems getting emacs to behave well with markdown and visual-line.  I don't want to chase that now, and will live without markdown preview.  I've added -*- mode: visual-line -*- to the top of README.md ... please preserve that.

There are a few stray mentions of /work still in the readme file and Dockerfile talks about /work

Other than that, it's looking very good and about ready to test

===

Is there a benefit to creating a user "claude" inside the container?  Would there be a drawback to using $USER ?

===

What about mounting the memory on /home/pace/.claude inside the container, so that the default paths for that will work?

I'll want to easily tell which shells are in the container.  Maybe add "SB" in red to the start of PS1.

Let's not duplicate the "docker build" command.  Have the rebuild argument just delete the old container and continue to build and launch a new container.

Make aisand detect and abort if it is run within an aisand container.

===

Ah, it's backslash-enter in this ui.

I just remembered about model choices.  We've done this work in Haiku 4.5.  I think I should change to a fancier model and ask it to review the work so far.  If I do that, will our conversation have continuity, or do I need to write stuff into memory first?

===

We've been designing aisand using the Haiku model and are just at the point of being ready to test.  I've just switch the model to Opus 4.7.  Please review the work so far and suggest any improvements that you notice.

===

Thanks.  I agree with your assessment, and in general, where you offered choices, I'm fine with your recommendation or ambivalent.  Since the files are just drafts now, I think you should make all the edits before the first test ... there's no point in testing stuff that is about to change.

I interpreted the original question about whether localhost access should be allowed as: "Do I need to let a program running in the container access http://localhost:3000" and my answer is no, that's not needed.  I don't think I feel sensitive to anything that a local service could spit out in an unauthenticated response.  So I don't need put a lot of effort into closing that path.  I'm fine with documenting the limit as a soft claim.

Regarding starting in /home/claude ... what is the point of the claude user in the container anyway?  Why not use $USER?

It does look like one image will be fine for the current usage plan.  aisand can evolve as I use it.

===

I've done "git init" in the aisand directory and committed an earlier version of the files.  Please commit the current files and write a commit message that will be helpful to future AIs who work on this project.  Then, I'll make the first test running in an ordinary host terminal.

===

Success.  I ran "aisand" in the directory /home/pace/apply (and old PHP web application), and I ran claude within that.  So far so good.  Here are some things to fix to smooth the startup (which we'll implement after we do some more testing):

pre-answer the initial questions:

Choose the text style
use the api key?"
security notes
trust in this folder

I've pushed aisand to https://github.com/pacew/aisand

I'm open to suggestions, but I imagine a good next step would be for you to write a prompt that I can paste into the claude session running in the apply sandbox to tell it to read https://github.com/pacew/aisand to understand that it is being used to test this setup.  Tell it to do a test sequence like:

create a branch aisand-test
create and run a small shell script
commit it to the repository
write some stuff to the memory in .claude

===

The test was very successful.  The main problem is we need to create .gitconfig with my name and email so git commit can work.  Also, I'd like to pre-answer claude CLI's questions about theme and confirming that it's running in a trusted directorry.  Also, pre-answer yes to questions like:

Allow reading from ~/.claude
Allow fetch from github.com
allow git checkout and mkdir
allow all edits for this session
allow commands similar to chmod and ./hello.sh
allow git add and commit
preload .gitconfig with my name and email
allow git config

I verified that the branch was created and committed and that the SB shell prompt worked.  Here is the report claude produced:

● Summary

  Worked:
  - Network: fetched the aisand README from GitHub via WebFetch.
  - File write: created aisand-test/hello.sh in the project directory.
  - Shell execution: script ran and printed env info (date
  2026-05-28T12:24:09+00:00, hostname aisand-apply, uid 1000).
  - Git operations: git checkout -b aisand-test, git add, git commit (commit
  961e230) all succeeded.
  - Memory write: created
  ~/.claude/projects/-home-pace-apply/memory/aisand-environment.md and
  MEMORY.md.
  
  Didn't work / needed setup:
  - Git identity is not configured anywhere in the sandbox — git commit failed
  with "Author identity unknown" until I set a repo-local user.name/user.email
  (per your "placeholder identity" choice: aisand-test <aisand@localhost>).
  Worth pre-seeding in the container if you commit regularly.

  Couldn't visually confirm:
  - The red SB marker in the shell prompt — I can't see my own terminal prompt;
  only hostname=aisand-apply and uid/gid 1000 indirectly confirm containment.

  Surprising:
  - The project's git status already had three untracked files (STATUS,
  current.sql, n.php) on the parent pace branch — left untouched as instructed.
  - The placeholder git identity is scoped to this repo only (.git/config), but
  .git/config lives in the bind-mounted project dir, so it will persist back to
  the host. You may want to git config --unset user.email / user.name before 
  pushing if that's unwanted.
  
  Branch aisand-test is ready for you to review with meld/git diff from outside
  the container.

✻ Cogitated for 1m 30s

※ recap: Goal was to validate the aisand sandbox by running a self-contained 
  test on branch aisand-test. The script committed cleanly as 961e230; next 
  action is for you to review the branch with meld/git diff outside the 
  container. (disable recaps in /config)

===

I reviewed your changes and they look good.  Let's run another test before committing.

I removed the untracked (and unimportant) files in the apply directory to avoid confusion.

I'm not very familar with claude memory.  The claude instance in the sandbox is still running.  I'd like it to remember what we've done so far after I close it, rebuild the container, and start a fresh session.  Is there something I need to do to tell it to write out state before I close the current session?

Then, please write a prompt with specific instructions for what the the new sandbox claude should test (including verifying that the memory was intact).

===

Looks good.  The startup was smooth and did not ask any environment questions.  I had to approve the first of each command type, but that's not too obtrusive - we can work on pre-approval in a future session.  It was surprised that the placeholder identity was gone ... I removed that myself, so no worries.

I think that's everything needed for baseline funcionality.  If you don't see anything else that should be fixed before committing the current changes to aisand, please go ahead and do that, writing the commit message specifically for a future you.

Here is the sandbox claude test report:

All checks done. Report:

  Memory recalled (5 entries): aisand sandbox uses /home/pace/apply (a real PHP
  app) as a test bed, only aisand-test* branches are safe to touch, the first
  test on commit 961e230 left two rough edges that the rebuilt container was
  supposed to fix (host ~/.gitconfig mount and pre-seeded ~/.claude.json), and
  the aisand source lives at /home/pace/aisand / github.com/pacew/aisand.

  First-run prompts: None. ~/.claude.json is pre-seeded (~22 KB) with
  numStartups, approved API key, and cached features.

  Git identity: Works from host gitconfig. ~/.gitconfig is mounted read-only
  with Pace Willisson <pace@alum.mit.edu>. No per-repo [user] override in
  .git/config. Commit 0774b9b on aisand-test2 is authored Pace Willisson 
  <pace@alum.mit.edu> — fix verified.

  Surprises:
  - The placeholder aisand-test <aisand@localhost> that the previous test wrote
  into /home/pace/apply/.git/config is no longer there — current local config
  has no [user] section. Either you cleaned it up between sessions, or it never
  persisted. Either way, clean state.
  - Memory volume survived the container rebuild intact — all 5 prior memory
  files were present and readable.
  
  Memory updated: aisand-environment.md now reflects the verified-working state;
   aisand-next-verification.md flipped from "pending" to "confirmed."

✻ Worked for 50s

※ recap: Goal: verify aisand sandbox fixes after container rebuild. Both fixes 
  confirmed working — host gitconfig mount and pre-seeded ~/.claude.json — with 
  a clean commit on branch aisand-test2 authored as the real host identity. 
  Next: you review the branch with meld from the host. (disable recaps in 
  /config)

===

I pushed to github and I'll try this out on a real project when I next get a chance to work on personal projects.  Please write out your memories of this work (to the host ~/.claude).  As I said, I'm new to memories.  Is there a standard initial prompt I should give you (whether running in the host aisand directory or a sandboxed project) to find relevant memories?

===

A final bit of work this morning.  Let's split most of README.md off into a .md file with implementation details, and have the readme be a short file that describes the aspirations of the project with some example usage and a link to the details.  The audience of the readme is someone who has maybe a year of experience with programming python after their undergraduate degree and who hasn't thought much about security beyond what was taught in their courses.
